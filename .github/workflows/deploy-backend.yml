name: Backend CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies & run tests
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install moto aws-sam-cli pytest
          pytest tests/

      - name: Build SAM template
        working-directory: backend
        run: |
          sam build

      # disable deployment intentionally disabled to avoid infra churn.
      # infrastructure is deployed manually via SAM.
      #
      # - name: Deploy SAM
      #   working-directory: backend
      #   run: |
      #     sam deploy \
      #       --stack-name visitor-counter-backend \
      #       --capabilities CAPABILITY_IAM \
      #       --no-confirm-changeset
